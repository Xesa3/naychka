<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- фон -->
    <solid android:color="@android:color/white"/>

    <!-- обводка -->
    <stroke
        android:width="2dp"
        android:color="#5CBDD3"/>

    <!-- скругление -->
    <corners android:radius="12dp"/>

    <!-- внутренние отступы -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp"/>

</shape>